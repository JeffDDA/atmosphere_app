import 'package:flutter/material.dart';

class ConditionFlags extends StatelessWidget {
  const ConditionFlags({super.key});

  @override
  Widget build(BuildContext context) {
    // Stub: empty persistent overlay positioned top-right
    return const Positioned(
      top: 0,
      right: 48,
      child: SizedBox.shrink(),
    );
  }
}
